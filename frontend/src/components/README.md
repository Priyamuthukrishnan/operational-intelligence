# Reusable Components Folder
Contains design system elements, dashboard widgets, and interactive visualizations.
- `ui/`: Core styled components (buttons, input layouts, badges).
- `charts/`: Recharts-based telemetry graph components.
- `dashboard/`: Custom components (escalations list, health dials, clustering views).
